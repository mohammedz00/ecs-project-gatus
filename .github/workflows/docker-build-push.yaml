name: Docker build and push image

on:
 push:
    branches: [main]
    paths: 
        - 'app/**'
        - 'Dockerfile'       
 workflow_dispatch:

permissions:
  id-token: write
  contents: read


# add trivy
jobs:
    docker-build-push-image:
      runs-on: ubuntu-latest

      steps:
        - name: checkout-code
          uses: actions/checkout@v4

        - name: configure-aws
          uses: aws-actions/configure-aws-credentials@v4
          with:
            role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
            aws-region: eu-west-1
        
        - name: login-to-ecr
          id: login-ecr
          uses: aws-actions/amazon-ecr-login@v2
        
        - name: docker-build
          env:
           REGISTRY: ${{ steps.login-ecr.outputs.registry }}
           REPOSITORY: zenudeen-gatus-ecr
           IMAGE_TAG: ${{ github.sha }}
          run:
            |
            docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG ./app

        - name: run-trivy-scan
          uses: aquasecurity/trivy-action@v0.36.0
          with:
            image-ref: "145523122776.dkr.ecr.eu-west-1.amazonaws.com/zenudeen-gatus-ecr:latest"
            format: 'table'
            exit-code: '1'
            ignore-unfixed: true
            vuln-type: 'os,library'
            severity: 'CRITICAL,HIGH'
        
        - name: docker-push
          env:
           REGISTRY: ${{ steps.login-ecr.outputs.registry }}
           REPOSITORY: zenudeen-gatus-ecr
           IMAGE_TAG: ${{ github.sha }} 
          run:
            |
            docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG


        # - name: build-tag-push-image-ecr
        #   env:
        #     REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        #     REPOSITORY: zenudeen-gatus-ecr
        #     IMAGE_TAG: ${{ github.sha }}
        #   run: |
        #    docker build -t $REGISTRY/$REPOSITORY:$IMAGE_TAG ./app
        #    docker build -t $REGISTRY/$REPOSITORY:latest ./app
        #    docker push $REGISTRY/$REPOSITORY:$IMAGE_TAG
        #    docker push $REGISTRY/$REPOSITORY:latest


